# Để trống khi muốn frontend gọi cùng origin (/api) và đi qua proxy của Vite.
VITE_API_URL=

# Chỉ dùng cho Docker/Vite dev server proxy.
VITE_PROXY_TARGET=http://localhost:5001

# Chỉ dùng cho report-service backend, không dùng trong frontend.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash
